# NutriCare 2 - راهنمای ساخت APK

این پروژه یک کپی کامل و آماده از NutriCare است که برای ساخت APK تنظیم شده است.

## 📦 محتویات پروژه

این پروژه شامل:
- ✅ تمام کدهای منبع (پوشه `src`)
- ✅ تمام dependencies نصب شده (پوشه `node_modules`)
- ✅ تنظیمات Android کامل (پوشه `android`)
- ✅ اسکریپت‌های خودکار ساخت APK
- ✅ تمام فایل‌های پیکربندی

## 🚀 ساخت APK

دو روش برای ساخت APK وجود دارد:

### روش 1: ساخت با retry خودکار (توصیه می‌شود)
```bash
cd /Users/amirtaha/Desktop/nutricare2
./build-apk-retry.sh
```

این اسکریپت در صورت بروز خطا، خودکار تا 20 بار تلاش می‌کند.

### روش 2: ساخت معمولی
```bash
cd /Users/amirtaha/Desktop/nutricare2
./build-apk.sh
```

## 📍 مکان APK

بعد از ساخت موفق، فایل APK در این مسیر قرار می‌گیرد:
```
~/Desktop/nutricare-v1.0.0.apk
```

## ⚙️ نیازمندی‌ها

- Node.js نصب باشد
- npm نصب باشد
- Java 17 نصب باشد (اسکریپت خودکار از Java 17 استفاده می‌کند)

## 📝 یادداشت‌های مهم

1. **Dependencies نصب شده**: نیازی به `npm install` نیست
2. **Android SDK**: از SDK نسخه 34 استفاده می‌شود
3. **React Native**: نسخه 0.73.2
4. **حجم APK**: حدود 18 مگابایت

## 🔧 کتابخانه‌های اصلی

- react-native: 0.73.2
- react-native-gesture-handler: 2.14.1
- react-native-screens: 3.29.0
- react-native-image-picker: 8.2.1

## ✅ تست شده

این پروژه با موفقیت build شده و APK آن آماده است.
